apiVersion: v1

kind: Service

metadata:
  name: smarthostel-frontend-service

spec:
  selector:
    app: smarthostel-frontend

  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.frontend.service.port }}

  type: LoadBalancer
